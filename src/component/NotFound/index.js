import React, { Component, PropTypes } from 'react';


export default class NotFound extends Component {
    static propTypes = {
    };

    render() {
        return (
            <div>
                Not found
            </div>
        )
    }
}